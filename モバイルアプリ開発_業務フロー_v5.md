# モバイルアプリ開発 業務フロー v5
### リサーチ自動下書き → Go/No-Go → 仕様(Fable 5) → Mobbin UI方針 → 独立AIレビュー → 実装+自動テスト(Sonnet 5) → CI監査 → 自動ビルド・TestFlight → 提出 → 公開後自動レポート

> **v5での変更点(v4から)**
> 1. フローを「文書」から「リポジトリ内の仕組み」に変えた。各STEPのプロンプトは
>    `.claude/commands/` のコマンドファイルになり、毎回のコピペが不要になった。
>    新しいアプリはこのテンプレートリポジトリを複製するだけで始められる。
> 2. STEP 0とSTEP 1を統合。リサーチの下書きはAIが行い、自分はGo/No-Go判定だけ行う。
> 3. 受け入れ条件(AC)を自動テストに変換する工程を実装依頼に組み込んだ。
>    手動確認は「実機スモークテスト」だけに縮小。
> 4. STEP 4.5の監査のうち機械化できる部分(npm audit・シークレット検出)をCIに移した。
> 5. STEP 6の前に「6-0: EAS Workflowsによる自動ビルド・自動TestFlight配布」を新設。
> 6. STEP 7のデータ収集(クラッシュ率・レビュー)を自動レポート化し、Slack通知に載せた。
>
> フローの中身(何を確認するか)はv4を踏襲。変えたのは動かし方。

---

## 人間がやること一覧(1アプリあたり合計30〜45分)

自動化してはいけない5点。逆に言えば、これ以外は全部AIとCIの仕事。

| # | 判断 | STEP | 目安 |
|---|------|------|------|
| 1 | Go/No-Go判定 | 0.5 | 10分 |
| 2 | 仕様書v1.0の承認(レビュー指摘の採用/却下) | 3 | 10〜15分 |
| 3 | 実機スモークテスト | 5 | 10分 |
| 4 | Apple/Googleでの提出操作・課金設定 | 6 | 5〜10分 |
| 5 | 審査却下時の対応方針決定 | 6-4 | 発生時のみ |

---

## 全体像

```
[初回のみ]  テンプレートリポジトリ複製・Mobbin MCP・EAS設定
     ↓
[STEP 0]   /step0-research → AIがresearch.mdを下書き
     ↓
[STEP 0.5] Go/No-Go判定(自分・10分)★人間ゲート1
     ↓
[STEP 2]   /step2-spec → Fable 5が確認質問1往復→spec.md作成
     ↓
[STEP 2.5] /step25-ui → Mobbin実例調査→UI方針メモ(ui-direction.md)
     ↓
[STEP 3]   /step3-review → 別セッションで独立AIレビュー
           → 指摘の仕分け・承認(自分)★人間ゲート2 → spec.md v1.0確定
     ↓
[STEP 4]   /step4-implement → Sonnet 5が計画提示→承認後、タスク単位で実装
           各ACに対応する自動テストも同時に書かせる
     ↓
[STEP 4.5] CIが自動監査(npm audit・gitleaks) + /step45-audit で観点チェック
     ↓
[STEP 5]   自動テスト全Pass確認 → 実機スモークテスト(自分)★人間ゲート3
     ↓
[STEP 6-0] mainへpush → EASが自動ビルド→TestFlight/内部テスト配布
     ↓
[STEP 6]   ストア審査向け対応 → 提出操作(自分)★人間ゲート4
     ↓
[STEP 7]   公開後7日・30日の自動レポート(Slack通知) → 読んで次を決める
```

**コスト方針(v4踏襲)**: Fable 5は仕様確定の1〜2往復のみ。実装・調査・レビューはSonnet 5または別モデル。

---

## 初回セットアップ(1回だけ・合計30〜60分)

1. **テンプレートリポジトリ**: このフォルダ一式をGitHubにテンプレートリポジトリとして登録。
   新アプリごとに「Use this template」で複製する。
2. **Mobbin MCP**(v4と同じ): Claude Desktopの Customize → Connectors →
   カスタムコネクタ追加 → 名前「Mobbin」、URL `https://api.mobbin.com/mcp` → 初回ログイン。
   Proプラン(月$10程度)推奨。
3. **EAS**: `npm i -g eas-cli` → `eas login` → `eas build:configure`。
   Apple Developer Program加入、App Store Connect APIキーを作成しEASに登録
   (これで提出まで非対話で回せる)。
4. **CI監査**: `.github/workflows/audit.yml`(同梱)をそのまま使う。push毎に
   npm audit とシークレット検出が走る。
5. **Sentry**: 無料枠でプロジェクト作成し、クラッシュレポートを受ける。
6. **公開後レポート**: 既存のGAS+Slack基盤に、App Store Connect APIのレビュー取得と
   Sentryのクラッシュ率取得を追加し、公開7日後・30日後に自動でSlackへ投稿するジョブを組む。

---

## STEP 0: 市場・競合・権利リサーチ(AIが下書き→自分は読むだけ)

`/step0-research アプリのジャンルと一言アイデア` を実行する。
AIがMobbin・ストアランキング・Web検索・レビュー分析を行い、`specs/research.md` を
v4のテンプレート(0-2の型分解、0-3のペインポイント、0-4の独自価値と権利)で埋めて返す。

自分の仕事は**読むこと**。「どれが自分のアプリに刺さるか」の取捨選択は、
次のGo/No-Go判定に集約する。

> 方針(v4踏襲): 学ぶのは問題設定・体験の型・マネタイズ仮説。名称・アイコン・画面の
> 見た目・文言・素材の流用はしない(App Store審査ガイドライン4.1・4.3)。

## STEP 0.5: Go/No-Goゲート(自分・10分)★

research.mdを読み、v4と同じ5項目でチェック。**4/5未満なら着手しない**。

```
[ ] 競合と比べて説明できる独自価値が一文である
[ ] 最初の価値到達までの時間が定義されている
[ ] 2週間程度で検証できるMVPの規模に収まっている
[ ] 権利・規制・審査上の明白な障害がない
[ ] 失敗した場合に何を学びたいかが明記されている
```

Goなら、research.mdの末尾にAIが自動生成した「STEP 1材料メモ」を確認・修正する
(v4のSTEP 1は独立工程としては廃止。中身はresearch.mdからの自動転記になった)。

## STEP 2: 仕様書作成(Fable 5・1〜2往復)

Fable 5のセッションで `/step2-spec` を実行(research.mdが自動で渡る)。
確認質問3〜5個に答えると、10項目のspec.md(v4と同一構成、ACはGiven/When/Then形式)が
`specs/spec.md` に保存される。

## STEP 2.5: Mobbinで UI方針確定

`/step25-ui` を実行。spec.mdの画面構成をもとにMobbinの実例パターンを調査し、
`specs/ui-direction.md`(採用する型・避ける型・自分のブランド表現)を作る。
気づいた仕様変更は必ずspec.mdに書き戻させる。

## STEP 3: 独立AIレビュー → 承認(自分・10〜15分)★

**別モデル・別セッション**(Codex CLIの `codex exec`、またはSonnet 5新規チャット)で
`/step3-review` を実行。v4と同じ6観点でBlocker/Major/Minorの表が返る。

自分の仕事: 指摘を「採用/却下(理由付き)」に仕分け、Blocker全解消を確認して
spec.mdを **v1.0** としてタグ付け(コミット)する。

## STEP 4: 実装+自動テスト(Sonnet 5・タスク単位)

新規チャット(またはClaude Code)で `/step4-implement` を実行。v4との違いは1点:

> **各ACに対応する自動テスト(unit/integration + MaestroによるE2E)を同時に書かせ、
> タスク完了報告に「対応AC・テスト実行結果」を含めさせる。**

これでSTEP 5の手動AC消化が不要になる。計画承認→タスク単位で進行はv4と同じ。

## STEP 4.5: 監査(CI自動 + AI観点チェック)

- **CIが自動で**: npm audit、gitleaksによるシークレット検出(push毎)
- **`/step45-audit` で別セッションに**: 未審査コード実行なし、WebViewラップなし、
  プライバシー申告との整合、削除導線、権限の最小化 — の観点チェックレポートを出させる
- 自分はレポートを読み、チェックを入れるだけ

## STEP 5: テスト確認 → 実機スモーク(自分・10分)★

1. 自動テスト全Pass(AC全消化)をCIの結果で確認
2. **実機で**主要フローを一周: 新規インストール→初回価値到達→再起動→オフライン→
   権限拒否→ダークモード・文字サイズ変更。ここだけは自動化しない
   (シミュレーターと実機の差・表示崩れは自動テストが苦手で、審査通過率に直結する)
3. 不具合は箇所を特定してSonnet 5に修正依頼(「全部見直して」ではなく具体的に)

## STEP 6-0: 自動ビルド・自動配布【v5新設】

mainへpushすると `.eas/workflows/build-and-submit.yml`(同梱)が走り、
iOS/Androidのビルド→TestFlight/内部テストへの提出まで自動で完了する。
処理後10〜15分でTestFlightに現れる。手元でXcodeを開く作業は無い。

## STEP 6: ストア審査向け対応(チェックリストはv4のまま)

6-1〜6-4のチェックリストはv4と同一(release-checklist.mdに複製して使う)。
ストア掲載文・スクリーンショット構成案・審査ノートの下書きはAIに作らせてよい。
**提出ボタン・課金設定・年齢制限申告はApple/Googleの管理画面で自分が操作する**★。

却下時: ガイドライン番号を確認してから、指摘箇所だけSonnet 5に修正依頼。
対応方針の決定は自分★。

## STEP 7: 公開後レビュー(自動レポート→読んで決める)

公開7日後・30日後に、GAS+Slack基盤が自動でレポートを投稿する:
- クラッシュ率(Sentry API)
- 新着レビュー全文と低評価の傾向(App Store Connect API)
- STEP 1で決めた成功指標の実測値(アプリ側の計測をSTEP 2の仕様に含めておく)

自分はSlackで読み、「優先修正/仕様変更/次のアプリへ」を決める。
大きな変更を検討する場合はSTEP 0の簡易版から再評価(v4踏襲)。

---

## リポジトリ構成

```
(テンプレートリポジトリ)
├── CLAUDE.md                     ← フローの要約・技術スタック・禁止事項(AIが毎回読む)
├── .claude/commands/
│   ├── step0-research.md
│   ├── step2-spec.md
│   ├── step25-ui.md
│   ├── step3-review.md
│   ├── step4-implement.md
│   └── step45-audit.md
├── .eas/workflows/build-and-submit.yml
├── .github/workflows/audit.yml
├── specs/
│   ├── research.md               ← STEP 0の成果物
│   ├── spec.md                   ← STEP 2〜3(バージョン管理)
│   ├── ui-direction.md           ← STEP 2.5
│   ├── decision-log.md           ← 実装中の判断記録
│   └── release-checklist.md      ← STEP 6用(v4の6-1〜6-4を複製)
└── (アプリ本体のコード)
```

フロー自体を改善したいときは、テンプレートリポジトリの該当ファイルだけ直す。
以後に複製するアプリすべてに反映される。

---

## 補足: 考え方(v4から不変)

- 仕様書は「何を・なぜ」。「どうやって」の細部は実装側(Sonnet 5)の領域
- ACをGiven/When/Then形式で先に決める。v5ではこれがそのまま自動テストになる
- 同じ会話の続きに自己レビューさせない。レビューと監査は必ず別セッション・別モデル
- 学ぶのは問題設定と体験の型。表現(UI・文言・素材)は自分のブランドで作り直す
